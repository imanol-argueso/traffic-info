export interface Camera {
    cameraId: string,
    sourceId: string,
    cameraName: string,
    urlImage?: string,
    latitude?: number,
    longitude?: number,
    road?: string,
    kilometer?: string,
    address?: string
}